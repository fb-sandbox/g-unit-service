# Test configuration for DynamoDB
# Using mock/offline mode for local testing
dynamodb.table.name=g-unit-service-test

# Quarkus Test Configuration
quarkus.log.level=INFO
quarkus.http.host=localhost
quarkus.http.test-port=0
