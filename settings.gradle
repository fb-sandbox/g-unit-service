pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
}
rootProject.name = 'unit-service'
// include 'integ'  // Integration tests require CodeArtifact token
